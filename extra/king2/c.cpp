#include <iostream>
#include <cmath>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
#include <boost/multiprecision/cpp_int.hpp>

using namespace std;
namespace mp = boost::multiprecision;

int main(){
	int n=1;
	cin >> n;
	vector<mp::cpp_int> a(n);
	vector<mp::cpp_int> b(n);

	for(int i = 0; i<n; i++) {
		cin >> a[i]
	}

	for(int i = 0; i<n; i++) {
		cin >> b[i]
	}

	return 0;
}

int count_swap_sort(mp::cpp_int *a) {
	for(int i = 0; i<n; i++) {
		cin >> a[i]
	}
}